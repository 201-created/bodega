{{#items-slider class='items-slider col p-0'}}
  {{#if isFastboot}}
    {{! loading ui }}
    <article class="show-item h-100">
      <button class='show-item__add-to-cart btn btn-large btn-success' data-test-add-to-cart>&nbsp;</button>
    </article>
  {{else}}
    {{#each model as |item|}}
      {{show-item item=item class='item'}}
    {{/each}}
  {{/if}}
{{/items-slider}}

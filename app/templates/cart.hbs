{{bodega-cart order=cart.order
              increment=(action 'increment')
              decrement=(action 'decrement')}}

{{to-elsewhere named="header" send=(hash component=(component "back-button") title="Cart")}}

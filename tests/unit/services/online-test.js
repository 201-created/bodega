import { moduleFor, test } from 'ember-qunit';

moduleFor('service:online', 'Unit | Service | online', {
  integration: true
});

// Replace this with your real tests.
test('it exists', function(assert) {
  let service = this.subject();
  assert.ok(service);
});

---
language: node_js
node_js:
  - "6"

sudo: required

addons:
  chrome: stable

cache:
  directories:
    - node_modules

before_install:
  - npm config set spin false
  - npm install -g bower
  - bower --version

install:
  - npm install
  - bower install

script:
  - npm test
